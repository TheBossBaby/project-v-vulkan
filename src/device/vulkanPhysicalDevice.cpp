#include <vulkanPhysicalDevice.hpp>

namespace projectv
{
    namespace vulkan::device
    {
        VulkanPhysicalDevice::VulkanPhysicalDevice()
        {
        }
        
        VulkanPhysicalDevice::~VulkanPhysicalDevice()
        {
        }
    }
}